/**
 * Dashboard page example
 * This page will use:
 * 1. Root layout (Header + Footer)
 * 2. Dashboard layout (sidebar + content wrapper)
 */
export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>This page uses both root layout and dashboard layout</p>
    </div>
  );
}

